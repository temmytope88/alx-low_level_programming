#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

char *_strstr(char *haystack, char *needle)
{
	while (*haystack != '\0')
		haystack++;
	while (*needle != '\0')
		needle++;
	return (0);
}
