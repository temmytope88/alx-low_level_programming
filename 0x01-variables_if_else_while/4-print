#include <stdio.h>
/**
*main - Entry point
*Return: Always 0 (success)
*/
int main(void)
{
	char g;
	for (g = 'a'; g <= 'z'; g++)
	{
		if(g == 'e' || g == 'e')
		{
			continue;
		}
		putchar(g);
		putchar('\n');
	}
	return(0);
}
